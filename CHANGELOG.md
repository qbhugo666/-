# 更新日志（Changelog）

> **版本里程碑速查（给用户记版本用）：`MILESTONES.md`**——8 个锚点记住全部历史；
> 2026-09-12 起发版规矩：只有实质变化才升版本号，小修攒着并入下一个里程碑（GUARDRAILS G 节）。
> 采用类似 GitHub 的标记：`feat`=新功能，`fix`=修复 bug，`perf`=性能/优化，`refactor`=重构。
> 2026-09-09 起采用语义化版本 + Keep a Changelog 纪律（见 GUARDRAILS.md G 节）：
> 修复累计为一个 PATCH、新功能为一个 MINOR、不兼容变更为 MAJOR；开发中的变更先记「未发布」区。

## [0.55.9] - 2026-09-17

### fix（水波涟漪四边中点被切平，「上面一段像被切断」，用户截图反馈）
- **病根**：涟漪画在 LogoCircleView 自己的画布上——硬件加速下每个 View 的绘制天生
  裁剪在自己的矩形边界内（`clipChildren=false` 只放行父级裁剪，管不到这一层），
  涟漪圆环只有四角附近的弧段能画出来，四边中点全是平直切口（2026-09-17 真机逐像素
  取证实锤：视图边界以上 0 像素，左右下方却有半径 232px 的环体）
- **治本**：涟漪改为画在直接父卡片（hero_idle，尺寸足够容纳全环）的 ViewOverlay 上，
  动画每帧直接 `parent.invalidate()` 驱动重绘——overlay drawable 的 invalidateSelf
  回调链真机探针实测不触发宿主重绘（draw 零调用），直调父级 invalidate 才生效
- 真机验证：原切口区（视图边界以上）修复前 0 像素 → 修复后稳定出现涟漪像素，
  圆环完整无切口；布局零改动，logo/文字位置不变

## [0.55.8] - 2026-09-17

### fix（设置改主题主页不变，须清后台重开才生效，用户实测复现）
- **病根**：主题只在页面创建那一刻套上（ThemedActivity.attachBaseContext 读偏好）；
  设置页选完主题只 recreate 了设置页自己，返回主页只是 onResume——没人重套主题，
  主页停在旧主题；清后台重开=页面终于重建才读到新偏好（2026-09-17 用户实测复现）
- **治本**：ThemedActivity 记住本页创建时生效的主题模式，onResume 时发现偏好变了就
  recreate()——一处改动，主页/设置/关于/使用记录/词典/绑定全部页面统一生效；
  偏好一致时零开销不闪屏
- 真机验证：浅→深返回主页立刻全黑、深→浅立刻变回，往返双向通过；页面重建不打断
  进行中的会话（顶部胶囊聆听态全程保持）

## [0.55.7] - 2026-09-17

### fix（启动连点永远起不来：加载中被重复点击反复打断，用户实测复现）
- **病根**：主页「开始控制」每点一次都会把会话代际 +1 并新开初始化线程——正在加载模型的
  线程发现被顶替，就把加载一半的模型弃掉重来；连点比加载快（SenseVoice 约 2.5 秒）时永远
  加载不完，水波涟漪转不停、会话起不来（2026-09-17 用户实测复现，红米真机日志铁证）
- **治本**：VoiceService 新增 initInFlight 在途标记（@Volatile）——启动加载期间的重复启动
  请求一律忽略并留痕「启动请求忽略：模型加载已在进行中（重复点击防抖）」，绝不重启加载；
  加载线程 try/finally 保证落地/夭折/失败都清标记；会话落地新增 SESSION_COMMIT 日志留痕
- 真机验证：0.3 秒 12 连发指令 = 11 忽略 + 1 落地；UI 连点 8 下（0.4s 间隔覆盖全程加载期）
  = 5 忽略 + 1 落地翻「正在聆听」；单点启动/结束/重开会话路径不受影响（gen 逐会话正常递增）

## [0.55.6] - 2026-09-16

### feat（水波涟漪替代按压缩放：用户实测纠偏）
- **回退 0.55.5 的卡片按压缩放**：用户实测纠偏——真实热区感知在圆形 logo 上，卡片整体
  缩放方向不对
- **圆形 logo 加水波涟漪**（LogoCircleView.startWaitingRipple）：点「开始控制」后两圈
  相位错开的水纹从圆边向外荡开渐隐（水=正在等待启动，1.6s/循环）；会话建立（renderPhase
  切卡）即停；授权/引导等提前返回分支也停——「还在等待」看得见，「好了」看得见
- 涟漪扩散半径收在英雄卡内缘消失（不越卡片）；hero_idle 加 clipChildren=false 放行
  越界绘制（属性微调，零新增元素）

## [0.55.5] - 2026-09-16

### feat（开始控制按钮按压动效：测试反馈「连点以为没反应」）
- 主页「开始控制」卡加 iOS 风格按压缩放动画：按下微缩 0.96、松手回弹——点击到会话真正
  建立有 1~2 秒延迟，测试朋友连点以为没反应；动效给「已收到」的即时确认
- 不消费触摸事件，点击逻辑原样；零布局改动、零新增元素（商用三关：主页面零新增）

## [0.55.4] - 2026-09-16

### perf（同命令冷却 1.5→0.8 秒：连发翻页更快，用户拍板）
- **同命令冷却 1.5s→0.8s**：连说两条一样的命令（连翻页/连缩放）第二条被拦的时间减半。
  澄清诊断结论：用户报的「左滑后右滑听不进去」真凶是**两句话间隔 <0.3s 被 VAD 合并成一段、
  匹配只执行先注册的第一条**——与冷却无关（不同动作互不冷却）；
  0.1 秒级连发需要「合并段多命令全执行」，因 v0.37 低音量误合并教训（点击二十五点击十八）
  对编号类危险，本期不做
- 1.5s 是抖音外放事故铁闸：压短后外放误触发可能多执行几次，熔断（8s 6 次）仍在兜底；
  回退条件=外放场景误触发变多改回 1500L（VoiceService.COOLDOWN_MS 一行）

## [0.55.3] - 2026-09-16

### perf + feat（触发灵敏度加强：半夜小声命令触发不了，用户拍板）
- **拼音模糊匹配放宽 0.5→0.65**（「牺牲一点误触率换触发率」）：半夜小声说话识别残缺
  （如「显示编号」只听清「死边号」，相似度 33% 被旧线拒绝）导致触发不了；
  误触有同命令冷却/连续误触熔断/「退出」最高优先三道保险兜底，回退条件=误触增多改回 0.5
- **数字同音字补缺**：耳→二、斯/思/撕→四、留→六（数字命令解析前生效，不影响听写/文字点击）
- **未匹配原文进诊断导出**（v0.48 诊断缓冲）：长按未命中/文字未命中/命令未匹配三处静默路径
  记入诊断事件——胶囊仍不显示（不增加挫败感），导出反馈可见「半夜那句话被听成了什么」，
  用真实听岔样本精填相似词表

## [0.55.2] - 2026-09-16

### perf（响应提速：说完到动手压掉 100ms）
- 正常灵敏度档（1~8 格）VAD 说完确认静音 0.4→0.3 秒：用户反馈「识别到动手还是慢」，
  这 0.4s 是纯等待（确认说完了不是换气），压掉 0.1s 每句更跟手（历史：0.5→0.4 v0.18 同因提速）
- 弱声专档（9~10 格）不动（0.55/0.7 秒）：构音障碍者字间停顿长，压缩有斩句风险
- 回退条件：真机出现句中停顿被拦腰斩成两句（识别出残句、命令没反应）→ 改回 0.35/0.4
  （`RecognitionSensitivity.minSilence` 一行）；顺带正效应：切句更勤可减少低音量下
  多条命令被合并成一段的旧问题（v0.37 实测过的「点击二十五点击十八」）

## [0.55.1] - 2026-09-15

### 修复（恶性：真「退出」被守卫反锁——用户被占麦困住，最高优先级）
- **案发**（用户日志实锤）：用户连说四次「退出」全部被 ASR 听成「走出」，v0.50.1 的误退出
  守卫四次全拦 → 无法语音退出 + 麦克风占用（此机型占麦时小爱唤不醒）= 用户最恐惧的场景。
  另澄清两件事：看门狗 9 分 46 秒释放为正确行为（只说了 1 次「继续」，预算 5+5=10 分钟；
  25 分钟需说满 4 次）；11:53:31 的新会话为家人开启（代码无自动开麦路径，胶囊只是会话镜子）
- **修复：守卫升级阶梯**——30 秒内近似退出被拦 **3 次**判定为真实退出请求、第 3 次放行
  （闲话不会连说三遍，被困者一定会）；第 2 次拦截时横条给出明确指引
  「想退出请说退出，或把刚才的说法连说三遍」；锁屏类模糊命中维持全拦不升级
- **预警文案带预算**：「即将休眠，说继续延长（剩余 N 次）」——总时长取决于说了几次继续，
  让预算看得见，不再误以为 25 分钟自动给满
- **导出补「诊断事件」段**：DiagnosticsHelper 环形缓冲（预警显示/延期/会话结束/守卫拦截等
  关键事件带毫秒时间戳）此前未接入导出，而本机 logcat 一小时即被冲掉——本次"预警到底
  显没显示"无法回溯正是此盲区；接入后此类问题永久可查

## [0.55.0] - 2026-09-15

### 新功能（媒体控制：说「播放/暂停」任何播放器通用，用户报告网易云播放点不到）
- **现场取证**：网易云播放页的无障碍树里，播放/暂停按钮的标记是二合一的**「播放暂停」**，
  同屏还有「播放模式」「播放列表」——说「播放」文字点击会命中多个候选，误点「播放模式」
  就"看起来没反应"（裘晨阳的听书 App 同类问题）
- **修复：播放/暂停升级为系统媒体会话命令**——走 `dispatchMediaKeyEvent`（等价耳机线控），
  **任何播放器通用，完全不依赖 App 给按钮做无障碍标记**；暂停时无活动媒体则回落文字点击旧通道
- **「重复一次」补齐文字点击**：文字点击成功也登记可重复动作（记录落点，重复=原位复点）——
  媒体播放器里「暂停」后说「重复一次」不再报"没有可重复的动作"
- 「播放/暂停」可绑定自定义说法；命令词自动进识别热词偏置

## [0.54.2] - 2026-09-15

### 变更（「继续」次数 2→4，单会话硬顶 15→25 分钟，用户拍板"四五二十"）
- 每段 5 分钟不变；「继续」上限 ×2 → **×4**（仅预警期有效的门禁规则不变）；
  总时长 = 5 + 5×4 = **25 分钟**封顶，到期前 20 秒预警不变
- 同步：帮助文案、关于页、GUARDRAILS A.2（安全参数以用户本次拍板为准）、FEATURES
- 弱声专档/极限远场（0.54.x）参数不受影响

## [0.54.1] - 2026-09-15

### 优化（10 格升级「极限远场档」，用户提议稍远距离也能操控）
- 10 格参数推到 Silero 安全边界：**增益 3 倍**（远场信号弱，近距离大声可能削波）、
  **VAD 门槛 0.30**（实践下限）、**停顿容忍 0.7 秒**（远场混响+距离停顿更散）、
  **切句 8 秒**、**最短开口 0.2 秒**（远场短促开口也能开门）
- 格 9 维持弱声专档（0.44/0.55s/5s）不变；1~8 格零变化
- 梯子定位：1~8 日常 → 9 弱声/构音不清病友 → 10 稍远距离/极弱声极限场景
  （近距离大声用 10 格会削波，属设计取舍，设置页已说明）

## [0.54.0] - 2026-09-15

### 新功能（9~10 格升级「弱声专档」，回应"灵敏度/准确率再高一点且不影响内放误识别"）
- **原理**：内放免疫来自 VOICE_COMMUNICATION+AEC（媒体声在进软件前被系统消掉），与灵敏度
  参数完全解耦——提灵敏度结构性不会放内放进来
- **1~8 格零变化**（默认档与现有手感原样保留）；**9~10 格升级为弱声专档**：
  - VAD 门槛：10 格 0.42→0.36、9 格 0.456→0.44（Silero 实践下限约 0.3，留余量）
  - 句间停顿容忍：0.4→0.55 秒（构音障碍者字间停顿长，0.4s 会把句子拦腰斩断）；≤8 格保持 0.4s 延迟不变
  - 超长句切句：3→5 秒（慢语速长句不被腰斩）；≤8 格保持 3s
  - **AGC 自动增益强制开启**（原机型默认关）：系统层把小声/含糊嗓音拉到正常音量；机型相关行为，
    实测不佳降回 8 格即一行回退
- 适用：声音小/构音不清（鼻音重，如裘晨阳/蔡磊式嗓音）的病友；代价是 9~10 格对环境声更敏感，
  由噪音词静默/冷却/熔断三道既有铁闸兜底，嘈杂环境建议 8 格以下
- 设置页灵敏度说明同步更新

## [0.53.1] - 2026-09-15

### 优化（「重复 N 次」识别率专项，用户报告五次/六次被听成一次、一次本身不灵敏）
- **根因**（代码核实）：重复命令纯正则解析、不在命令词表——整个重复家族（重复一次~十次、
  一次~十次、两次、再来一次，共 21 词）在热词表里**零覆盖**。识别器无偏置时解码器偏爱
  常见搭配「重复一次」，真人含糊发音的五次/六次就被它抢走
- **修复**：REPEAT_HOTWORDS 21 词全部进热词表（8.0 分偏置，与「抖婴→抖音」同机制）；
  数字同音字表补「词/此→次」（量词被听岔的纠正）
- **验证（如实说明）**：TTS 合成音频基线 5/5 全对（合成音太干净，复现不了真人口播的模糊）
  → 修复后复测 5/5 仍全对（无回退）；热词表确认收录（重 复 五 次 等 10 条「重复 ×」在列）。
  真人口播的改善幅度需用户实测反馈

## [0.53.0] - 2026-09-15

### 新功能（语音截屏，用户提议）
- 说「**截屏**」（或「截图/截个屏/屏幕截图」）→ 触发系统全局截屏动作（Android 9+）：
  系统自带的截屏动画、编辑器、保存全由系统接管，等价于电源键+音量下
- 老设备（Android 9 以下）不支持时明确提示「需要安卓 9 及以上」而非无反应
- 「截屏」进可绑定动作清单——口齿不清的用户可绑定自己的说法；新命令词自动进识别热词偏置

## [0.52.1] - 2026-09-14

### 修复（抖音点不到底栏编号「没有编号 32/33」，用户报障；真机实锤定位出两个根因）
- **根因一（屏幕参考系偏小）**：无障碍服务上下文的 displayMetrics 比物理屏矮一截——
  抖音底栏 edge-to-edge 元素（bounds 铺到屏幕物理底边）的中心被误判「屏幕外」而拒绝点击
  （真机 logcat：编号 8 在屏幕外（bounds=(927,2415)-(1268,2756)））。修复：编号点击/文字点击
  的体检与坐标夹取全部改用 realScreenRect()——API 30+ 取 maximumWindowMetrics（物理屏含
  手势条区域），更早回退 getRealMetrics；负坐标体检（下午的防崩溃）原样保留
- **根因二（显示与点击各数一遍）**：「显示编号」画徽章数一遍、点编号时重数一遍——信息流
  元素实时增删（实测 25+→8 个），编号对不上账。修复：所见即所点——显示时存「编号→屏幕位置」
  快照，点击/长按优先按快照定位，徽章随翻腾实时重画=快照始终与屏幕一致；无快照才走旧现遍历
- **顺修假成功**：doTapLabel 旧版派发即返回 true，没点上使用记录也记「✅ 已执行」——
  改为主线程内联执行、如实返回（拿不到窗口/越界/复验失败都不再记 ✅）

## [0.52.0] - 2026-09-14

### 变更（静音自动释放删除，用户拍板：时间制独挑大梁）
- 60s 安静就断太激进——会话时长完全由看门狗管理：每段 5 分钟、预警 20 秒、「继续」×2、
  硬顶 15 分钟。安静不再提前断会话（想早退说「退出」/锁屏即可）
- 删除项：SILENCE_RELEASE_MILLIS、silenceRunnable 及全部 8 处挂载点；同步帮助文案与关于页

### 修复（「继续」没到预警也延期，裘晨阳报 bug，代码核实属实）
- **原问题**：handleExtendSession 只查延期名额不查预警状态——会话开始 10 秒说「继续」
  也会白扣名额 + 重置 5 分钟计时 = 提前浪费
- **修复**：「继续」仅预警期（到期前 20 秒）有效；早说明确拒绝并告知「还有 X 分 Y 秒到续期点」，
  名额不扣、计时不重置（CONTINUE_EARLY 日志留痕）；segmentStartElapsed 跟踪段起点

## [0.51.0] - 2026-09-14

### 新功能（会话退出全记录 + 黑匣子异常终检，用户提议"飞行记录仪"）
- **背景**：用户排查「时间没到为何自动退出」时发现使用记录里完全没有退出痕迹——会话死得无声无息
- **每次退出聆听留一条使用记录**：「会话结束：{原因}（本次 X 分 Y 秒 · 延期 n/N 次）」，
  四种原因（识别到「退出」/看门狗强制释放/静音自动释放/锁屏自动释放）全走 releaseAndStop
  统一出口；使用记录页与导出反馈均可见；logcat 同步 SESSION_END 行（底层严格记录）
- **黑匣子异常终检**：会话出生在偏好留档（session_active_since，进程死亡也在）、正常结束销档；
  下次会话启动发现档还在 = 上次没落地（被系统杀/崩溃，当时无法记录）→ 自动补记
  「⚠️ 上次会话异常终止（开始时间，无结束记录）」。与 CrashCatcher（崩溃全尸）互补：
  正常退出有痕、崩溃有全尸、无声被杀有追认
- 守卫：仅真实会话记使用记录（SIMULATE 注入/重复释放不制造噪音）；黑匣子档案走
  SharedPreferences 静默记录，不占使用记录
- **活体检查**（真机验收 A 组抓到并修正）：终检须 `!recording`——会话进行中收到停止/重开/注入时
  档案属于活会话，不是孤儿，不得误报"异常终止"

## [0.50.1] - 2026-09-14

### 修复（闲话被拼音模糊匹配掰成「退出」，会话无辜断开——用户本人日志实锤）
- **案发**：用户日志 18:44:17 识别到闲话「走出」→ pinyin_fuzzy 距离 1 命中「退出」→ 红线释放麦克风。
  用户感知"说完发送后自动退出聆听"（18:43:26 发送后一次 + 18:44:17 一次，两次同源；麦克风重启记录佐证）
- **病根**：「退出」是保护词——自定义指令/词典都不许绑，但内置模糊纠错层自己放行了近似音触发，
  前门上锁后门敞着
- **修复**：dispatchMatched 顶部守卫——exit_session（退出）/lock_screen（锁屏）两条会话终结类命令
  拒绝 pinyin_fuzzy 命中，静默忽略（记日志+诊断环形缓冲，含识别原文）
- **退出通道全保留**：原文含「退出」红线、精确、包含、同音字拼音精确——口齿不清用户照常能退；
  守卫放执行层单一漏斗，matchFuzzy 纯函数与 JVM 测试不动

## [0.50.0] - 2026-09-14

### 新功能（自定义指令可绑定数字：点击编号 N / 点击第 N 格，用户提议+亲自定交互）
- **绑定页动作列表末尾新增「选择数字」二级入口**（50 个数字不摊平列表）：点开先选类型
  （点击编号 N 1~50 / 点击第 N 格 1~12），再在数字大按钮网格（5 列点选，不打字不报数）选数字，
  然后照常语音录自己的说法。网格只到 12——3×4 一层就 12 格，绑 13+ 永远点不到=坑用户
- **执行与原生完全对齐**：命中数字绑定走同一执行函数（tapLabel/tapGridCell），「重复一次」
  支持、横条文案、✅ 使用记录口径一致；网格未显示时明确提示「说显示网格」而非点屏幕中心
- **三处开绿色通道**：CustomBindings 校验放行数字动作+displayAction 展示名（确认框/列表显示
  「点击编号 7」而非内部代号）；CommandMatcher 数字绑定自成条目前插（自定义优先不变）；
  备份卡导出/导入自动兼容（走同一 upsert 校验门）
- 长按类数字绑定本次不做（用户只要求点击）

## [0.49.1] - 2026-09-14

### 修复（点屏幕外目标崩溃断线：朋友裘晨阳三次崩溃实锤；静予「打开桌面上没有的软件就自动下线」同源）
- **根因**：点击文字/编号、长按文字/编号四条链路（textCenter/labelCenter→tapAt/longPressAt）全程
  无坐标体检——目标图标在屏幕外（MIUI 桌面旁页/负一屏，getBoundsInScreen 返回负值）时，
  GestureDescription 构造抛「Path bounds must not be negative」FATAL，进程死亡=语音会话陪葬，
  用户感知「用一半自己停了」；该通道绕过冷却/熔断，重试必炸（裘晨阳 16:17/16:35 三次同签名
  崩溃全发生在说「点击优酷」瞬间；使用记录的 ✅ 写在点击执行前=假成功）
- **修复两道防线**：①病根处——textCenter 中心点必须在屏幕内，越界跳过候选/返回 null 走静默
  忽略分支（顺带治好屏幕外假成功）；labelCenter 越界返回 null 走「没有编号 N」提示
  ②保险丝——tapAt/longPressAt 坐标 clamp 屏幕内 + Path 构造包 runCatching（与滑动/缩放风格一致），
  坏点最多点不中，永不炸进程
- 不自动重试（既定原则）；滑动/缩放/网格天然安全未动

## [0.49.0] - 2026-09-14

### 新功能（设置页「备份」卡：导出/导入个人配置，用户提议换机免重设）
- **导出个人配置**：自定义指令 + 个人词典 + 3 项偏好（识别灵敏度/深色模式/震动反馈）打包成
  带版本号的 JSON 文字（`_v:1`，人也能读），自动复制到剪贴板 + 弹系统分享——发微信
  「文件传输助手」/收藏即永久存档；换手机、重装 App 粘贴回来即可，不用重新设定
- **导入是「合并」不是「替换」**（用户拍板）：逐条走现有校验器（CustomBindings.upsert /
  CustomVocab.validateWord + 指令撞车检查、50 词上限、保护词拒收），只增改、绝不删除
  手机上已有数据；无效条目跳过并计数，完成后汇报「导入 X 条、跳过 Y 条」
- 深色模式被导入改动 → 整页自动重载；自定义指令导入后按文件时间戳自动重建匹配器，
  无需重启会话；听写纠错即时生效
- **边界**：不含设备相关数据（无障碍授权/滑动学习缓存/使用记录/崩溃记录）——换机本就要重来
  或涉及隐私；导入文本设防御上限（20 万字符/指令 300 条），不是本软件的配置明确拒绝
- UI：设置页「服务」与「关于」之间新增「备份」卡，两行克隆现有行模板

## [0.48.2] - 2026-09-14

### 修复（深色模式「选了不变黑」，用户报告又坏了）
- **根因**：ThemedActivity.applyDarkPref 用错掩码常量——清的是 `UI_MODE_TYPE_MASK`（类型位）
  而非 `UI_MODE_NIGHT_MASK`（夜间位），旧夜间位残留与新值叠加成非法配置（0x30）→
  values-night 资源永不匹配。**系统浅色时选「深色」必现不生效**；系统开自动日落切换时
  表现为时好时坏。自 v0.33.0 深色模式引入即存在，与 v0.48.1 装机无关
- **修复**：一行——`UI_MODE_NIGHT_MASK.inv()` 只清夜间位再放新值（类型位保留），
  深色/浅色/跟随系统三种模式全场景正确

## [0.48.1] - 2026-09-14

### 修复（胶囊文案与看门狗常量不同步，用户报告「胶囊还显示 2 分钟」）
- **根因**：v0.35.0 把看门狗改成 3 分钟时，「继续」延期路径的两条胶囊文案还是写死的旧数字
  （「已延长 2 分钟」「已达最长 10 分钟」），常量变了文案没跟上 → 实际已 3 分钟、胶囊仍报 2 分钟
- **治本**：两条文案改由 `WATCHDOG_MILLIS / EXTEND_MILLIS / MAX_EXTENSIONS` 推导，
  以后调整时长胶囊自动跟随，此类不同步从根上消失

### 变更（看门狗节奏放宽，用户拍板）
- 每段 3 分钟 → **5 分钟**，喊「继续」次数上限 4 → **2**（5 分钟 × 3 段，硬顶仍 **15 分钟**）
- 同步：关于页看门狗说明（每 5 分钟续期、最多 2 次）、FEATURES 家底表

## [0.46.0] - 2026-09-13

### 新功能（悬浮图标自动移除，用户反馈「悬浮窗影响使用」）
- **ensureWeChatCompat 加第三步**：确保双组件在场后，若「无障碍按钮」指派指向随选朗读
  （右侧悬浮图标的来源）→ 自动清空指派——**授权设备（照护者 adb 一次）全自动无悬浮窗**；
  服务启用与微信编号不受影响（真机实测：双组件自动恢复+微信编号 20 元素正常+图标消失）
- 无授权设备：引导路径不变（微信支持弹窗两步：开随选朗读→手动取消按钮指派），
  悬浮图标不影响功能，仅视觉

## [0.45.0] - 2026-09-13

### 新功能（「微信支持」一键引导入口，用户指出无电脑用户开不了随选朗读）
- **设置页「服务」分组新增「微信支持」**：未开启时点击 → 说明弹窗 →「一键前往」直达系统
  「随选朗读」开关页（深链接优先直达详情页，失败退回无障碍列表并提示）；开启后返回 App
  状态自动变「已就绪」——把「装完新版还差一步」的最后一步缩到最短，无电脑用户自助完成
- 安卓安全底线说明：任何 App 都不能静默打开他人的无障碍开关（防恶意），故有电脑授权的设备
  走全自动（App 代为写入），无授权设备走本引导——两条路覆盖所有用户
- 验证：设置页新行与状态展示、弹窗、深链接跳转真机截图

## [0.44.0] - 2026-09-13

### 新功能（微信编号兼容自动化，用户反馈「别的手机微信不显示编号」后定位：随选朗读未在场）
- **随选朗读自动保活**：每次服务启动时检测系统「随选朗读」（谷歌无障碍套件）——已安装但未启用
  且我们持有 WRITE_SECURE_SETTINGS 时，自动并入无障碍开关（幂等，不删他人服务）。
  微信 8.0.52+ 只把界面树交给「场上有可信读屏服务」的设备，此机制让编号能力不再依赖
  用户记得手动开另一个开关；HyperOS 清后台/撤开关后下次启动自动恢复
- **默认强制、零用户决策**（用户拍板）：不设开关；最坏情况=多开一个系统自带无障碍服务
  （实测零可见副作用），可随时在系统设置手动关闭（下次会话自动恢复——设计意图）
- **品牌适配边界（如实说明）**：随选朗读属谷歌无障碍套件——小米/红米中国版实测内置；
  无谷歌服务的品牌（华为/荣耀等）需改用其自带读屏服务，逐品牌真机验证后纳入（待办）
- **导出反馈新增「无障碍状态」段**：已启用服务清单 + 本服务绑定状态 + 随选朗读在场状态
  ——远程诊断微信编号问题不再依赖猜测
- 触发与边界：VoiceService 全启动路径覆盖；仅写入无障碍开关（既有边界不变）

## [0.43.1] - 2026-09-13

### 修复（使用记录每次进程重启清零，用户导出反馈中「使用记录：无记录」暴露）
- **根因**：UsageLog 持久化写的是数组套数组格式（[[时间,文本],...]），init 读取却按对象
  字段 {"t","x"} 解析——格式不匹配，加载必失败被静默吞掉 → 每次进程重启历史清零
- 修复：init 兼容两种持久化格式（数组套数组现行 + 对象格式历史兼容），单条解析失败跳过、
  不整批丢弃；persist 维持现行格式

## [0.43.0] - 2026-09-13

### 新功能（问题反馈诊断升级，用户指出「导出的日志抓不到崩溃证据」——结构性缺陷属实并补齐）
- **崩溃记录器 CrashCatcher**：注册全局未捕获异常处理器——崩溃瞬间把堆栈/线程/机型/内存水位
  写入本地（保留最近 5 份），记录后原样交还系统（闪退行为不变，只多留现场）；
  MainActivity/VoiceService 双入口幂等注册
- **导出新增三段**：①内存画像（总/可用内存、低内存模式、isLowRamDevice、应用堆）
  ②崩溃记录（无则「无」）③系统错误日志（`-b crash` 崩溃缓冲 + `*:E` 错误级最近 200 行，
  AndroidRuntime/DEBUG/低内存杀进程记录不再被过滤）；原使用记录/应用日志段不变
- **开发期崩溃触发后门**（CRASH_TEST，商用前移除）：adb 注入测试空指针验证记录→导出链路
- 背景：用户朋友 Note 12（22101320C/HyperOS 2.0）闪退，旧导出结构上抓不到崩溃证据；本轮补齐后
  朋友升级新包→复现→导出回传，即可按堆栈对症修（OOM→低配稳定包/native→按点修/FGS→补引导）

## [0.42.1] - 2026-09-13

### 变更（两个功能入口改名，用户反馈不易理解）
- 「常用词」→ **「个人词典」**（输入法行业通用叫法：搜狗/百度/Gboard/iOS 均为此口径），
  副标题「人名、地名优先识别」；页标题与说明同步
- 「自定义说法」→ **「自定义指令」**（小爱同学/iOS 快捷指令通用口径），
  副标题「用自己的话下指令」；页标题同步
- 纯文案改名，机制、数据、页面结构零改动

## [0.42.0] - 2026-09-12

### 新功能（自定义常用词/个人热词，用户提出：说 huangxin hao 自动识别成「黄信豪」）
- **设置页「语音」分组新增「常用词」**：把人名、地名等加为个人热词——听写落笔时，
  识别原文里与常用词**拼音相近**的片段自动改写为该词（如识别出「王信豪」→ 落笔「黄信豪」），
  听写/聊天全线受益；保存后下次会话生效；上限 50 词
- **生效机制（TTS 对照实验如实记录）**：合成「黄信豪」音频实测——热词文件正确并入该词后
  greedy 解码结果不变（「王信豪」），证明 SenseVoice+greedy 下热词偏置实际无效（此前「8.0 vs 2.0
  结果一致」的 A/B 亦吻合此解释）；故常用词的实际生效层=**听写落笔时的拼音模糊纠错**
  （CustomVocab.correctText，纯函数 JVM 可测），该层与解码解耦、可靠可控。
  热词文件保留（换解码器/模型时可复用）
- **三层防干扰（用户明确要求）**：①独立名单独立文件；②指令匹配零改动（常用词只作用于听写落笔）；
  ③**撞车拦截**——与指令词/别名发音相近的词准入即拒（CommandMatcher.commandCollision，
  如「话动」撞「滑动」），名单里永远不会出现能抢指令的词
- **录入=语音优先零打字**：识别原文预填 + 可编辑确认框（同音错字照护者改一次即可，可重新录入）+
  实时校验提示；保护词「退出/继续」与语气词不可加
- 验证：JVM 单测（validateWord 拒绝分支 + commandCollision 撞车 + correctText 纠错四例）；
  真机：TTS 对照 + 听写落笔「王信豪」→「黄信豪」dump 闭环 + 指令干扰回归全过

## [0.41.0] - 2026-09-12

### 新功能（语音替换：「把X替换成Y」，听写编辑二档）
- **输入框内容听岔纠正**：说「把不错替换成很好 / 把X换成Y / 把X改成Y」（X、Y 各 1~10 字）——
  精确找词 → 改写 → 回写 → 光标落在替换段末尾；替换后说「重复」等既有指令照常
- **拼音模糊滑窗找词**（CommandMatcher.findFuzzyRange）：屏幕上是「不错」、照读却被听成「不措」时，
  拼音相同即命中——念屏幕上显示的错字即可定位；找不到原词明确提示「输入框中没有『X』」，不静默
- 听写模式下说出「把A替换成B」按字面落笔（听写内容优先于命令拦截）
- 指令说明「文字输入」区新增条目；验证：JVM findFuzzyRange 三例 + 真机微信聊天页
  精确/中段/模糊/未命中四例 dump 闭环

## [0.40.0] - 2026-09-12

### 新功能（文字输入与听写编辑，对标 iOS 听写；可行性先经 TEXT_PROBE 真机实证）
- **小米式一次性短听写（用户拍板设计）**：会话中说「输入」或「听写」→ 立即进入录入 →
  说出内容 → 停顿（VAD 分句）即自动填入当前输入框光标处——不做「开始/退出听写模式」往返，
  面向短输入聊天场景；12 秒无内容自动退出；说「取消」放弃；「退出」红线在听写中仍然最高优先
- **文字编辑四动作**（新增词表组 text_edit，commands.json v7）：「光标左移/右移」（光标真实回退/前进）、
  「删除」（删光标前一字）、「清空输入」（清空输入框）——实现链=读文本→改写→SET_TEXT 回写→
  SET_SELECTION 恢复光标（TEXT_PROBE 已实证微信聊天输入框全通，随选朗读共存下树完全可见）
- 四个动作均可被「自定义说法」绑定（绑定标签已加入）；执行失败有明确提示（未找到输入框/输入框为空），
  不静默；指令说明新增「文字输入」分区
- 验证：JVM 单测（新增 text_edit 匹配 3 例）；真机微信聊天页闭环（听写落笔→dump 验文字→删除→光标→清空）

## [0.39.3] - 2026-09-12

### 变更（无障碍引导文案按行业口径定稿，用户两轮反馈）
- 标题精简为「开启无障碍服务」；正文按无障碍类应用的专业范式重写为三段——
  ①用途+数据承诺（合规标准句式「该权限仅用于实现语音控制功能，不会收集或上传您的任何信息」）
  ②路径引导（去开启直达 / 搜索「无障碍」或「辅助功能」/「已下载的应用」/ 不同机型路径略有差异）
  ③保活建议（行业标准语「锁定后台，保持后台长期运行」）
- 删除口语段落（「这是 Android 的安全要求…」「长期使用建议：…语音控制可长期在线」）与
  「各品牌」字眼；约 200 字 → 约 120 字
- 参考：GKD 官方引导、Google Play 无障碍权限用途申报要求、应用商店审核合规指引

## [0.39.2] - 2026-09-12

### 修复（无障碍引导路径全品牌通用化，用户指出小米专属写法不适用商用）
- **无障碍引导弹窗路径重写**：旧文案「设置 → 更多设置 → 无障碍」为老版 MIUI 专属，
  实测小米当前路径是「辅助功能 → 权限管控 → 已下载的应用」、原生安卓为「无障碍 → 已下载的应用」，
  各品牌均不同——改为通用写法：点「去开启」直达 + 「系统设置顶部搜索『无障碍』或『辅助功能』，
  在『已下载的应用』分组中开启」（搜索法为全品牌兜底）
- **自启动弹窗去品牌**：删「小米手机还需要…」，改为「建议允许『自启动』权限…」，并注明
  非小米机型无此选项时点「已开过了」继续——商用不再被品牌绑死
- 关于页「小米」为用户亲撰官方文案（事实性陈述），保持原样未动

## [0.39.1] - 2026-09-12

### 修复（音量动作「重复一次」无效，用户报障）
- **根因**：可重复动作白名单建于 v0.4，早于 v0.17 设备控制组——volume_up/volume_down 从未
  登记，执行成功后不记入「上一次动作」，重复时无东西可放（或错误重放更早的旧动作）
- **治本（用户拍板）：白名单废止**——任何派发成功的动作都记入「上一次动作」，
  「重复一次/N 次」回放的就是上一个动作，**以后新增功能天然可重复、无需登记**
- 安全边界不变：退出走红线不经派发（exit_session 另加防御性排除）；重复上限 10 次、
  间隔 620ms、音量 80% 硬帽、熔断/冷却照旧；回放绕过冷却/熔断为既有设计（明确指令≠自动重试）

## [0.39.0] - 2026-09-12

### 新功能（自定义说法绑定，用户提出：「动作自适应用户想表达的词」）
- **设置页新增「自定义说法」页**：把用户自己的说法绑到既有动作上，全程语音录入、零打字——
  选动作 → 语音录入（走正常会话链路，安全机制全在，听到第一句自动结束）→ 确认保存 → **立即生效**
  （无需重启会话）。场景：说「向上滑动」总被识别成「乡上花东」的用户，绑定后说这句话就执行
- **引擎接入**：自定义说法 = 目标命令的外挂别名（commands.json 词表本体零改动），
  保存即参与四层匹配并**自动进入 ASR 热词偏置**（识别端会越听越贴用户自己的说法）；
  与标准说法同分时自定义优先（用户显式意图）；绑定前插实现于 CommandMatcher.fromJson
- **边界与安全**：说法限 2~10 个汉字、语气词拒收、保护词「退出」「继续」不可绑；
  **退出红线不经过匹配层**——自定义词永远挡不住退出/求救；同一短语重复绑定=覆盖（最新意图优先）；
  一个动作可绑多个说法；列表点击删除、删除即失效
- matcher 从 lazy 改为按绑定文件时间戳自动重建；录入会话=正常会话+「捕获下一句」标志
  （onRecognized 捕获原文后立即还麦）
- 验证：JVM 单测 7 例（自定义命中/包含/优先/热词/未知动作跳过/无绑定零变化）；
  真机 SIMULATE 闭环（预置绑定→说自定义说法→匹配执行；标准说法回归）

## [0.38.0] - 2026-09-12

### 新功能（识别灵敏度滑块，用户为声音小/口齿不清的病友提出；新发版纪律下首个实质里程碑）
- **设置页「语音」分组新增「识别灵敏度」滑块（1~10 格）**，默认 5 = 作者日常基准
  （恰好等于历史常量，默认零行为变化），下次会话生效。不放主页（商用红线：主页零新增）
- **双旋钮联动（用户拍板）**：往高——软件增益最高 2.25×（安卓无公开麦克风硬件增益 API，
  数字放大是"功率更强"的等价实现，小声话喂给识别引擎前先放大）+ VAD 说话门槛降到 0.42
  （极小声/口齿不清才"开得了闸"）；往低——增益 0.5× + 门槛 0.80（嘈杂环境防误触）。
  两旋钮缺一不可：只放大不降门槛，极小声开不了闸；只降门槛不放大，听到了也认不准
- **崩溃风险=零**：滑块只是会话开始时读一次的浮点数，无结构变化；高灵敏度最坏情况是
  误识别变多，由既有三道保险兜底（音量 80% 硬帽/1.5s 冷却/8s≥6 次熔断，抖音事故铁闸）
- 新增 `RecognitionSensitivity.kt` 纯函数映射（格5=1.0×/0.60 硬约束，注释写明勿破坏）；
  识别循环增益后钳制 ±1 防爆音；VAD 日志带门槛值便于排查
- 验证：回声/离线测试台 3 点实测（格5 回归+格10 有效性），中间格数学上必然介于其间；
  **最终验收=目标用户本人**（拖到舒服的格，一分钟自调）

## [0.37.7] - 2026-09-12

### 变更（首次引导弹窗增补「加锁后台」建议，用户提出）
- **无障碍引导弹窗（商用普通用户必经路径）末尾新增保活建议**：在最近任务中长按
  「言出法随」卡片、点击弹出的小锁锁定后台（部分机型为下滑卡片出现锁图标）——
  手势表述对齐小米官方客服说法与华为官方教程句式，兼容两种机型手势；
  加锁=不依赖电脑的最强保活（清空后台不再杀进程→无障碍不再被撤），与自启动弹窗
  已有的括号提醒形成首启链双保险。电池弹窗不加字（0.36.1 精简拍板不动）

## [0.37.6] - 2026-09-12

### 修复（清空后台后无障碍自愈失效，用户报障「重开 App 也不恢复」）
- **根因实证：adb install -r 重装会丢 WRITE_SECURE_SETTINGS 授权**（当日 14:05 自愈日志
  「自愈跳过：未授权」实锤；此前档案误记「一次性授权永久有效」）。现场已重新 `pm grant` 恢复，
  授权恢复后 force-stop→重开实测：自愈写回开关→系统自动重绑→Bound services 双双在列
  （**推翻「单写不重绑、必须两段式」的猜测**——Crashed 名单为空时单次写入即可重绑）
- **自愈兜底升级**：整条开关串被清空的极端情况下，恢复时自动带上系统「随选朗读」
  （检测到 com.google.android.marvin.talkback 已装才带；Manifest 增 queries 包可见性声明）——
  防微信编号因白名单缺位而静默失效；正常「外科手术式」撤开关（只删自己、他人保留）走原只加不删分支
- **HANDOFF 重装必跑清单增补 pm grant 行**（每次 adb 重装后必须重新授权，否则自愈沉默失效）

### 配置（微信编号打通，零代码方案，同日）
- **微信里「显示编号/点击编号」已可用**：机制=微信 8.0.52+ 白名单只认「任一可信服务在场」，
  与系统「随选朗读」（Select to Speak）双服务共存即放行真树。对照实验+报号点击闭环全部
  实测通过（主列表页 20 节点；点 18 → 通讯录页真实切换+编号自动刷新）。**本项无代码改动**，
  仅手机侧双组件开启 + `_test` 两个脚本/HANDOFF/FEATURES 同步（重绑必须双组件一起写，
  只写本服务会悄悄弄丢微信编号）。随选朗读开启实测无悬浮按钮、无手势劫持等副作用
- 伪装类名方案（服务改名 SelectToSpeakService）研究结论：可行但未采用——条款灰色+上架
  审核风险；共存方案零风险且更抗微信版本变化（微信不敢把系统服务踢出白名单）

## [0.37.5] - 2026-09-12

### 变更（编号徽章样式，用户拍板「改回之前几个版本的黑色圆角矩形」）
- **「显示编号」徽章恢复 v0.8 经典样式**：黑底（85% 黑）白字、20dp 圆角正方形、5dp 圆角、
  11sp 白字——几何参数逐项取自 releases/v0.8-src-baseline 原始代码，非凭感觉重画。
  此前 0.15 起改成的 iOS 深色圆徽章（0.37.4 还短暂加过白描边）不再用于编号
- **如实更正 0.37.4**：其变更记录声称「移除白色描边」，但提交代码中网格与编号两处 ringPaint
  均仍在绘制——白描边从未真正移除。本版一并处理：编号换回圆角方块；网格徽章保持 iOS 深色圆
  并真正摘除白描边（回到 0.37.2 状态）
- 编号收集 v2（可点击 + 带文字叶子）与残留防护（空结果移除+限次重试+落定复查）零改动

## [0.37.4] - 2026-09-12

### 精修（编号徽章样式，用户反馈）
- **移除白色描边**（用户反馈丑），恢复纯深色圆徽章 + 白字原样式
- 编号收集 v2（可点击 + 带文字叶子）与残留防护（空结果移除+限次重试+落定复查）全部保留
- 附：徽章不依赖系统昼夜配色——深色圆徽章在浅色页（用户的微信现状）对比度最高

## [0.37.2] - 2026-09-11

### 修复（编号残留的真正根因：切页竞态）
- **编号绘制后新增 600ms「落定复查」**：从桌面切微信的过渡期里，刷新可能拿到的还是
  旧窗口的树（非空，不走重试）→ 画出一遍桌面编号 → 之后微信页面静止不再有事件 →
  错版永久定格（用户实测残留）。现改为：绘制时记录所在应用包名，包名变化则 600ms 后
  再刷新一次纠正；包名相同（已落定）即停止，不循环
- 显示/隐藏命令会复位复查状态；页内滚动不受影响（仍由内容变化事件驱动刷新）

## [0.37.1] - 2026-09-11

### 修复（编号残留，用户实拍报障）
- **修复「桌面的编号残留在微信」**：编号刷新拿到空结果（页面切换瞬间拿错窗口/树未就绪）时，
  旧编号覆盖层原样保留——错位的桌面编号就糊在了微信上。现改为：
  **空结果立即移除旧覆盖层 + 限次重试（最多 3 次、每次 400ms）**，重试仍空才保持无编号
- 显示/隐藏命令与隐藏操作都会复位重试计数，杜绝无限循环
- 配合 0.37.0 的收集 v2，编号体系全部复查完毕：收集规则回归（v0.26.0 引入的纯 clickable 收集）
  与残留刷新两处历史问题均已闭环

## [0.37.0] - 2026-09-11

### 修复（微信聊天页不显示编号，用户报障）
- **编号收集升级 v2**：微信聊天页的消息气泡在无障碍树里不标记 isClickable，
  纯「可点击」收集在该页结果为空——一个编号都画不出来（真机复现实锤）
- 对标苹果 Show numbers（给所有可见元素编号）：收集规则改为
  **可点击节点 + 带文字的叶子节点**；被可点击节点完全包含的文字不重复编（原页面编号不漂移），
  同边界去重；显示与点击定位共用同一收集器，「看到的编号=点到的编号」不变
- 实测：微信主页编号数量与规则不变；聊天页气泡两侧出现编号，可点击气泡

## [0.36.1] - 2026-09-11

### 精修（电池授权引导，用户反馈太啰嗦）
- **电池优化引导弹窗重绘**：系统 AlertDialog → iOS 卡片式自定义弹窗（对齐深色模式弹窗审美，
  深浅自适应）；文案从三段长文精简为两句话——「无障碍服务需长期存活，语音才能随叫随应 +
  请在接下来的系统弹窗中点『允许』，一次授权、长期有效」；按钮改为 暂不（灰）/ 去允许（蓝）
- 拒绝提示同步精简：「已跳过：无障碍可能被系统自动关闭」

## [0.36.0] - 2026-09-11

### 新功能（记录保留期 + 问题反馈导出，用户提出）
- **使用记录 48 小时自动清除**：加载/写入时自动清理超期记录（隐私优先 + 控制存储占用），
  100 条上限不变；记录页顶部新增保留期说明文案
- **新增「导出」按钮**（使用记录页顶栏，清空左侧）：一键打包问题反馈文本——
  导出时间 / 版本 / 设备与系统 / 48 小时内使用记录 / 本应用最近 400 行日志，
  走系统分享面板（微信/短信等）发给开发者，对标小米式 bug 反馈

## [0.35.2] - 2026-09-11

### 变更（关于页官方文案，用户提供全文）
- **关于页全文替换为用户亲自撰写的官方文案**：品牌头副标（系统级·全离线·零数据上传·中文原生）
  + 五个编号章节——01 项目背景 / 02 关于命名 / 03 核心特性 / 04 安全设计 / 05 项目愿景，
  底部落款「黄信豪 Hugo」
- 唯一编辑：文案中看门狗数字按 v0.35.0 现状更新（每 3 分钟续期 / 最长 15 分钟），其余一字未改
- 文案注：旧「关于命名」段中「间隙≈0.5% 拟合」等历史描述随全文替换而移除；logo 资产仍以
  design/logo-spec.png 抠图为准（LOGO_STANDARD.md）

## [0.35.1] - 2026-09-11

### 精修（指令说明专业化，用户反馈）
- **指令说明全部重写为正式教程文案**：命令以标准说法呈现（向上滑动/向下滑动/向左滑动/
  向右滑动/轻点/双击……），说明改为完整教程句式；口语说法（上滑/左滑等）移入
  「使用说明」注明同样可识别
- 新增「双指缩放」「双击」条目；修正过期的时长说明（10 分钟 → 15 分钟）
- **修复指令说明弹窗深色模式渲染**：标题/分隔线/命令文字的写死颜色换语义资源——
  此前深色下命令文字（#1C1C1E）在黑底上几乎不可见

## [0.35.0] - 2026-09-11

### 变更（看门狗预算放宽，用户拍板）
- 会话时长：基础段 2 分钟 → **3 分钟**，「继续」仍 ×4，硬顶 10 分钟 → **15 分钟**
- 实测：会话开始后第 180 秒整弹出休眠预警，20 秒后强制释放，时间分毫不差
- 静音 60s 自动释放、熔断、冷却等安全线全部不变——只放宽总预算，结构不动

## [0.34.0] - 2026-09-11

### 新功能（双指缩放，对标苹果 Zoom in/out）
- **新增「双指放大 / 双指缩小」**：两指水平对称、屏幕中心为轴、500ms 匀速捏合——
  看图片/网页时放大看细节、缩小回原样；别名：放大/缩小/图片放大/捏合放大等
- **桌面/系统页拒绝保护**（与摇移同款）：双指捏合在桌面会误触编辑态，桌面/系统页横条提示拒绝
- 支持「重复 / 重复 N 次」连续缩放；指令说明弹窗与关于页均已补充说明
- 词表 version 6（pinch_zoom 组）

## [0.33.1] - 2026-09-11

### 精修（深色模式三处打磨，用户逐图反馈）
- 设置页组顺序调整：语音（识别语言）在最上，外观（深色模式）移到其下
- **深色模式下主页左上角换白色方块图标**：规范图「浅色模式（主）」原样抠图
  （drawable-night-nodpi/tile_logo.png），黑底上清晰可见；浅色模式仍是黑色方块
- **深色模式选择弹窗卡片化重绘**：20dp 圆角卡（深浅自适应）+ 居中标题 +
  三行单选（右侧品牌蓝勾/灰环）+ 蓝色完成胶囊——替代毛坯房系统弹窗

## [0.33.0] - 2026-09-11

### 新功能（深色模式）
- **设置页新增「外观 → 深色模式」**：三选项——跟随系统（默认，随手机昼夜自动切换）/ 浅色 / 深色，
  选中立即整页生效（ThemedActivity 基类 createConfigurationContext 覆盖 uiMode，无需 AppCompat）
- **全量黑色主题**：所有页面颜色收敛为语义色资源（values 浅色 + values-night 深色）——
  AMOLED 纯黑底 #0B0B0B + 深灰卡 #161618 + 深蓝调英雄卡 #12203A；品牌蓝圆与状态色昼夜共用
- 动机：半夜打开软件白屏刺眼（用户提出）；已实测系统深色下自动跟随生效

## [0.32.4] - 2026-09-11

### 修复（无障碍服务故障事故，用户点名）
- **修复「此服务出现故障」+ 开始控制后胶囊不弹**：SIMULATE 测试注入以前台服务身份启动但从不调
  startForeground，Android 14+ 立刻崩掉整个进程（ForegroundServiceDidNotStartInTimeException）——
  无障碍服务同进程陪葬、被系统标故障，胶囊（挂在无障碍上）自然弹不出。日志实锤：两次 FATAL 均在
  注入后 16ms，此前「MIUI 杀后台」归因作废
- 修法：单发注入先合法前台化（startForeground），执行完 stopForeground+stopSelf；
  实测同场景复验：进程存活、崩溃日志零新增、胶囊正常弹出、故障标记清空

## [0.32.3] - 2026-09-11

### 修复（桌面图标闪变）
- **修复退出 App 返回桌面时图标「变一下」**：MIUI 桌面不认 layer-list 的 item 定尺寸，
  把自适应图标前景位图拉伸铺满画布 → 桌面渲染成黑块（用户实拍确认），与退出动画的系统渲染不一致，
  切换瞬间即闪变。改为**密度位图前景**（ic_launcher_fg.png，图形 40dp 缩放已烤进 PNG，五档密度），
  任何渲染路径同一张图。另：adb 重装后跑 `am force-stop com.miui.home` 刷新桌面图标缓存

## [0.32.2] - 2026-09-11

### 修复（logo 改用设计图原始资产）
- **彻底放弃代码手绘 logo**：此前两版手绘（细线 chevron / 比例拍脑袋）都不像，
  用户两次点名（「变成两个球了」「太近太边」）。改为从设计规范图连通域抠取原始像素：
  - 新增 `ic_logo_glyph_white.png`（圆点+实心箭头原样抠图，含抗锯齿 alpha）
  - `LogoCircleView`：品牌蓝 **#246BFE**（规范印制值）正圆 + 图形位图，图形宽 56.7% 直径（规范方块实测）
  - 顶栏小方块改用规范「深色模式」方块原样裁切 `tile_logo.png`
  - 桌面图标改为规范方块生成的 mipmap PNG + v26 自适应（位图前景 40dp/108dp，保留主题图标）
  - 删除全部手绘 logo 矢量（ic_logo_circle/ic_launcher/ic_launcher_foreground 旧版/ic_logo_glyph/bg_logo_tile）
- **LOGO_STANDARD.md 二次修订**：核心铁律=资产抠图、禁止手绘；脚本 make_assets*.py 可复现

## [0.32.1] - 2026-09-11

### 修复（品牌图形对数）
- **logo 图形全面按设计规范对数**（圆点直径 32% + 间距 12% + 箭头宽 56%）：
  此前圆点与箭头间距只有约 0.5%（几乎贴死）、箭头宽度只有规范一半——用户点名「太近了、跟真正设计的不一样」
-四处实现同步修正：LogoCircleView（英雄卡/关于页大圆）、ic_launcher（桌面图标）、
  ic_launcher_foreground（自适应前景）、ic_logo_glyph（顶栏小方块，改回专用图形并铺满 32dp）
- **新增 `LOGO_STANDARD.md`**：品牌几何唯一标准（含放入容器规则与改动纪律），并写入 AI 长期记忆——改 logo 先改标准再动代码

## [0.32.0] - 2026-09-11

### 变更（关于独立成页 + 齿轮补全）
- **新增「关于」页**：真正的软件介绍——品牌头 + 是什么 / 能做什么 / 隐私承诺 / 支持四张卡。
  此前主页底部「关于」错跳设置页（与齿轮同目的地），设置里的「关于」只是三行弹窗（用户点名）
- **主页底部「关于」、设置页「关于言出法随」统一改跳关于页**；设置页保留「请作者喝杯咖啡」直达爱发电
- **设置齿轮重画为八齿完全对称**（同一矩形旋转 45° 步进 + evenOdd 圆环）——
  此前用的现成 Material 路径左下天生缺口（用户点名「缺一块」）

## [0.31.0] - 2026-09-11

### 变更（对标设计稿收尾）
- **主页状态卡四态闭环**：执行完成后自动落地「已完成」（绿勾，停留 1.2s）再回聆听态——
  此前卡在「正在执行」转圈不回落；带 ✅ 才算完成，模式切换等提示不冒充失败红叉
- **品牌圆徽章改 Canvas 自绘**（LogoCircleView）：数学正圆，替代矢量 drawable 拉伸
- **设置齿轮换 Material 标准路径**：此前不像齿轮（用户点名）
- colors.xml / strings.xml 分仓修复落库（上次误混排的收尾）

## [0.30.0] - 2026-09-11

### 变更（主页对标设计稿全量重构）
- 主页三段式：顶栏（logo 方块+标题+齿轮）/ 英雄区双态（开始控制 ↔ 四态状态卡）/ 指令+使用记录双卡
- 新增设置页（语音/反馈/服务/关于）、使用记录页（中央挂钩 SessionState.lastMatch，100 条上限）
- 震动反馈开关（默认关，执行成功短震 30ms）；齿轮打开设置页而非直跳系统设置

## [0.29.1] - 2026-09-10

### 变更
- **胶囊尺寸变化不再做任何过渡动画**：交给布局系统一帧完成自然拉伸——
  实测 scaleX 与 ChangeBounds 两种过渡都会产生中间畸变（用户感知「矩形变来变去」「弹动生硬」），
  全部移除。圆角由超大半径恒定保证，任何宽度一帧到位
- 声波增益提升至 3×，非活跃时平滑淡出至完全消失并停帧（零开销）

## [0.29.0] - 2026-09-10

### 变更
- IslandBar 全量重写：单状态机 applyState + ChangeBounds 唯一过渡 + 实时声波视图
  （该版过渡方案在 0.29.1 中被判定为过度设计并移除，声波与状态机保留）

## [0.28.1] - 2026-09-10

### 变更（主页完成度精修）
- 品牌头图：logo 小方块 + 标题横排（页面的完成度来源）
- 咖啡入口从大列表行降级为底部小链接（不再占据正中间）；底部新增版本号
- 行密度收紧：行高 54→50dp、图标 29→26dp

## [0.28.0] - 2026-09-10

### 变更（主页第一性原理重构）
- **移除「识别：/匹配：」调试信息**——那是开发期的东西，不是用户界面
- 状态卡改为「彩色圆点 + 一句话」：绿点=就绪，蓝点=会话中
- 「请作者喝杯咖啡」从页脚散字升级为正式列表行（暖咖杯图标，与其他行同构）
- 底部新增版本号显示；两枚列表图标圆角统一为同一构造（22%）

## [0.27.4] - 2026-09-10

### 修复
- 主页「退出会话」的警报红图标突兀 → 改为石墨灰电源符号（语义=「结束」而非「警报」）
- 卡片圆角 16→18dp、行文字 17sp→16sp regular（更秀气）、主按钮字重 bold→medium、间距统一

## [0.27.3] - 2026-09-10

### 修复
- **「正在聆听…」文字误显示在胶囊里**：v0.27.2 重构时误删了聆听态的隐藏文字路由——现在恢复，
  聆听态只显示麦克风+声波（截图放大取证确认）
- **声波小点「·····」贴着文字**：安静时声波完全隐藏（iOS 原则：识别到声音才有波浪），
  有声音时随振幅淡入；显示文字时声波同步隐藏

## [0.28.2] - 2026-09-10

### 修复
- 胶囊「弹动有矩形感」：内容变化时宽度瞬间跳变——加入 iOS 岛式宽度形变（从旧宽度比例平滑展开）
- 会话开始/结束浮层瞬间出现/消失——改为 200/180ms 淡入淡出
- **桌面说「滑动」误入桌面编辑模式**：系统页滑动时长 380→280ms，压回长按阈值之下（截图实证）

## [0.28.1] - 2026-09-10

### 修复
- 桌面说「滑动」误入桌面编辑模式：系统页滑动时长 380→280ms，压回长按阈值之下（截图实证）
- 「触发时像框/空气墙」：胶囊+横幅两个圆角块叠成一个矩形块——撤销横幅元素，**文本直接长在
  单胶囊里**（iOS 岛式 morph：聆听=小胶囊，执行=同胶囊变宽显示文字，预警=琥珀胶囊），
  任意状态只有一个完整胶囊

## [0.27.1] - 2026-09-10

### 修复
- 声波振幅偏小：显示端增益 1.8×、波形条加宽加高
- 命令横条为 18dp 圆角矩形、与整体胶囊语言不符 → 改为超大圆角完整胶囊

### 性能
- 声波重绘限频 ~30fps（原为屏幕刷新率全速），仅会话聆听期运行，降低会话期功耗

## [0.27.0] - 2026-09-10

### 新增
- **实时声波指示（对标小米语音控制）**：胶囊内五段圆角声波随说话实时起伏——识别循环每 100ms
  回传真实麦克风振幅，攻击快/释放慢的平滑曲线，静默时收拢为呼吸小点；替代旧的静态呼吸

### 修复
- 胶囊圆角在文字展开时「变直」：固定 25dp 半径改为超大圆角自适应高度，任意尺寸保持完美胶囊

## [0.26.0] - 2026-09-09

> 对应历史快照名 v0.15~v0.25.5（全部包含在本版本内）。本版本起启用 SemVer + git 仓库。

### feat 新功能
- **精准摇移**：上/下/左/右轻划微调（短边 16%、520ms 慢拖），与滑动（42%、380ms 惯性轻扫）明确区分尺度；反馈为顶部胶囊朝拖动方向微移应答（不遮挡内容）
- **设备控制**：增加/降低音量（媒体音量 80% 安全硬顶 + 系统音量面板可视反馈）、静音、锁屏（自动结束会话还麦）、通知中心、控制中心
- **无障碍自愈**：电脑一次性授权 WRITE_SECURE_SETTINGS 后，App 静默修复被系统撤销的无障碍开关（实测 HyperOS 强杀会连开关一起撤销）；PC 侧矩阵脚本同步新自愈
- **主页 iOS 设置风 UI**：大标题 + 白卡片分组 + 蓝胶囊主按钮 + 列表行彩色圆角图标（红停止/蓝问号）
- **指令说明卡片式弹窗**：分区结构化排版（替代纯文本 AlertDialog）
- **品牌 logo 图标**：用户提供设计稿（design/logo-spec.png），像素级拟合 IoU 0.956；自适应图标 + API24 降级 + 纯白通知图标
- **咖啡入口**：页脚「请作者喝杯咖啡」→ 爱发电 afdian.com/a/hugoqb

### fix 修复
- **滚动「一格一格」（二轮根治）**：多段 continueStroke 手势链真机被系统静默丢弃→废弃改单笔划轻扫（380ms 带惯性）；**主线程时序 bug**——命令链与滚动事件回调共用主线程，当场 sleep 检查必然读到未派发旧值→校验改 TYPE_VIEW_SCROLLED 事件流（运行时订阅）+ 异步化
- **分享面板点击被自动取消**：点击校验的自动补发第二击对「选中类」控件等于取消选择（幂等性红线入档）→ 彻底移除自动补发
- **摇移光效不同步**（动作先出现光效后出现）：动画 post 队列晚一拍 + 减速插值与系统线性播放不一致→同步启动 + LinearInterpolator
- **「重复一次」被听成「过一次/不一次」**：宽松重复兜底（窄口径：短句+次/遍/是结尾+无动作动词+有可重复动作）
- **「点击第N格」任意层级直接点**（旧正则掉进缩放通道）；网格点击纳入可重复（记录落点原坐标再点）
- 编号模式移除遗留淡网格线（与「显示网格」命令混淆）

### perf 性能
- VAD 说完确认 0.5→0.4s（响应提速，若句子被斩断回退 0.45/0.5）；识别线程 2→4
- 滚动校验等待按手势时长校准（旧 200ms 等待对新时长手势是误判源，已随校验重构消除）

### refactor 重构
- 动作附加提示通道 actionNote 通用化（音量/摇移提示不被通用文案顶掉）
- 流光胶囊与手势几何同源（SwipeSegment 唯一来源）
- 工程约束成文：GUARDRAILS.md（六道流程关卡+代码红线+测试纪律）

## v0.14

### feat 可靠性架构（闭环执行器 A+B，用户拍板「以自动化代替真人实测」）
- **A 点击闭环**：文字点击/编号点击后抓全窗口结构指纹（64 节点抽样），250ms 后复抓，没变 = 假成功 → 自动补发坐标手势点击。与滚动假成功同源同治
- **B 滚动机制学习缓存**：试探学到的「该页用标准滚动/手势」存 filesDir/scroll_mech_cache.json（键=包名+页面+方向），已学习页面零试探延迟直达；缓存机制失效时仍回退手势，页面改版自动适应
- **自动化矩阵测试机**：`E:\VoiceControl\_test\scroll_matrix.py`（PC 驱动真机：逐 App 冷启动→SIMULATE 注入上/下滑→前后 dump 对比→CSV 报告），验证不再依赖真人

## v0.13

### fix 修复（假成功病，抖音消息页实测）
- **抖音消息页无法上滑**：日志每次都报「标准滚动成功(纵): RecyclerView」，但页面纹丝不动——与最近任务页同款假成功：performAction 返回 true ≠ 页面真滚了。对照实验：同一页面手动 input swipe 每次都能滚，标准滚动多次前后 dump 完全一致。修复：标准滚动派发后用「容器首/末子项屏幕坐标指纹」校验，150ms 内坐标没变 = 假成功 → 自动补发手势滑动，保证命令必定生效

## v0.12

### fix 修复（商用体验：误识别静默原则，用户确立）
- **用户报「返回」被听成「喂」→横条报「当前页面没有喂」**：误识别被明晃晃展示给用户，商用大忌。四条未命中路径全部静默化：①纯语气词（喂/嗯/呃…18个）直接忽略，不重置看门狗但重置静音计时 ②屏幕文字找不到→不操作不提示 ③长按文字找不到→不操作不提示 ④完全未匹配→不回显 ASR 原文。原则：识别对了才反馈，没对就安静继续聆听。日志仍完整记录供诊断

## v0.11

### fix 修复（真机日志实证）
- **休眠预警被冲掉→用户莫名断开**：预警出现后，任何命令的「执行反馈→复位」都会把预警文案顶掉（复位固定回「正在聆听…」）。用户全程看不到预警，以为突然断开（21:19:59 预警出现→21:19:59.363 被复位顶掉→21:20:18 断开，全程 19 秒无预警可见）。修复：预警期间复位时恢复预警文案。看门狗机制本身（2分钟/「继续」延期×4/10分钟硬顶）按用户设计原样保留，一字未动

## v0.10

### fix 修复（真机实测发现）
- **网格与编号浮层重叠**：两层各自独立绘制，互不排斥，同时开启会叠在一起。改为互斥：说「编号」自动收网格，说「网格」自动收编号
- **数字丢字「点击十八→点击八」**：真人实测 12 句，规律=动词+数字粘连时丢「十」，单独念的完整数字词极稳。把「十一～九十九」整词加入热词表（不做两步式，保留一步直给）

## v0.9

### fix 修复（P0 安全，全部真机验证）
- **多条命令被合并时执行了旧编号**（用户报「说点击十八却点了别的」）：说话音量偏低时 VAD 会把多条命令合成一段——离线静音 ASR 测试台实测到「点击二十五点击十八」（-12dB 以下必现）。而四个数字提取函数都用 `find()` 取**第一个**匹配，于是去点了 25。改为 `lastMatch()` 取**最后一个**（第一性原理：一句话里多条同类命令，最近说出的才是当前意图）。真机静音验证：注入「点击二十五点击十八」→解析 18 ✅、「点击八点击十三」→ 13 ✅、「长按五长按十二」→ 12 ✅
- **抖音回声：系统省电策略每 10 分钟杀进程→无障碍被解绑**（商用核心：用户抱怨「用一会又要重新授权」）：实测本机 HyperOS 默认省电策略 =「智能限制后台运行（推荐）：后台超过 10 分钟后关闭」，且 deviceidle 白名单为空。新增启动链两道系统级授权引导：①电池优化豁免（一键跳系统框选「无限制」）②MIUI 自启动授权；真机验证三环推进成功，`dumpsys deviceidle whitelist` 确认 `user,com.voicecontrol.app,10381` 已入白名单
- **抖音视频声音被误识别成命令**（安全事故级）：根因实锤——本机 audio_effects.xml 只给 voice_communication 挂了 aec/ns/agc 前处理，而代码用的 VOICE_RECOGNITION 一个都没有。修复：改用 VOICE_COMMUNICATION 音频源 + 显式挂 AEC/NS/AGC。回声测试台真机对比：修复前 25 秒误识别 3 条（「向上滑动」「左滑动」「停顿」）→ 修复后 12 秒外放零误识别，日志实证 AEC=true NS=true（AGC 由系统前处理链自动提供）
- **桌面永久挂「正在聆听」死横条**：横条可见性错误地挂在无障碍服务生命周期上（服务常开→横条永挂且不可操作）。改为「横条=会话的镜子」：会话开始才显示、结束必消失、服务重连时按会话状态同步。真机三断言验证：无会话0%/会话中99%/STOP后0%

### fix 修复（P0 稳定性，全部真机验证）
- **并发初始化 OOM**：快速连点「开始」会起多个后台加载线程并发创建 SenseVoice（每个约 200MB，实测修复前 3 个线程同时创建）→ 瞬时内存翻倍可能 OOM 闪退。改用「会话代际 + 初始化互斥锁」：同一时刻只允许一个线程创建重模型（实测由并发 3 个降为串行），被顶替线程就地释放资源
- **释放时序 use-after-free**：识别线程可能在 native 层 decode（数百 ms），原先 join 仅 500ms 不够，主线程提前 release recognizer 会崩溃。改为后台 teardown 线程 join(3s) 后再释放（实测 8 轮退出/重启零崩溃）
- **遍历函数无上限 ANR**：`collectClickable` / `collectByText` 原先无限递归，巨型页面会卡死主线程。补上深度/节点数上限

### feat 新功能（无障碍长期存活保障，商用核心诉求：几天不掉线）
- 启动链新增两道系统级授权引导（一次授权、长期有效）：①电池优化豁免（防 MIUI 省电杀进程→无障碍被解绑，实测本机白名单为空）②MIUI 自启动授权（进程被清理后系统可自动拉起）；授权页返回自动续接下一环
- 注：实测发现 adb force-stop 会清空系统无障碍启用列表（Android 官方行为）——之前测试期间频繁「无障碍掉线」很大部分由此造成，已禁用该命令

### test 测试基建（静音、可重复，不拿真机当试验场）
- **离线 ASR 测试台**（ACTION_ASR_TEST）：把 wav 直接喂给识别器，不碰麦克风、不出任何声音；自建临时 recognizer/vad 避开 native 并发。可传 hotwordsScore 参数，一个 build 内完成 A/B
- 用它做了两组静音量化实验：①信噪比梯度（SNR +15/+10/+5/0 dB，TTS 语音混背景噪音）②音量梯度（-6/-12/-18/-24/-30 dB）——定位到「低音量→VAD 合并命令」这个真 bug
- **测试音频全部隔离到 `src/debug/assets`**（正式包不含，省 6MB）；测试台均带安全约束：回声外放单次播放+12秒硬超时兜底，绝不循环

### feat 新功能（无障碍商用级引导闭环）
- 点「开始会话」前自检无障碍：未开启时弹引导框（讲清为什么需要 + 开启路径），一键跳转系统无障碍设置
- 从设置返回自动续接：开启后回 App 自动开始会话，无需再点开始
- 启动链路重排：麦克风权限 → 无障碍自检 → 自动开始（全就绪时零点击）
- 会话中无障碍被系统关闭时，横条立即提示「无障碍已关闭」（不再静默失效）
- 新增 AccessibilityHelper：标准 API 检测服务启用状态，兼容 MIUI/HyperOS 短名存储格式

## v0.8

### fix 修复（P0 安全与正确性）
- 释放麦克风时序：释放不再依赖 stopSelf→onDestroy 异步链，锁屏/看门狗/退出触发后立即 release AudioRecord；先 join 识别线程再释放，消除主线程 release 与识别线程 read 的竞态
- 初始化竞态：新增 stopRequested 标志，「开始后立刻退出/锁屏」时后台加载线程不会在完成后重新抢占麦克风
- 编号假成功：「点击15」但屏幕只有 12 个元素时不再闪「已执行」，横条明确提示「没有编号 15」
- 模型加载阻塞主线程：226MB 模型改后台线程加载，消除启动 ANR 风险（加载完才占麦）
- 录音权限被拒：不再空转开会话，界面明确提示去设置开权限

## v0.7

### feat 新功能
- 会话时长延长：看门狗 1 分钟 → 2 分钟；倒计时结束前 20 秒横条提示「即将休眠」
- 「继续」延期：说「继续」每次延长 2 分钟，最多 4 次（总时长最多 10 分钟）；达上限后拒绝，强制释放——防止 bug 自动无限延期导致彻底占麦
- 编号标签改黑底白字圆角小方块（更小更统一）
- 网格一步式点击：网格显示时说「点击 N」直接点第 N 格（不再只能先缩到最小再「轻点」）
- 锁屏自动释放：手机黑屏（SCREEN_OFF）立即释放麦克风，锁屏状态下随时能唤起小爱

### fix 修复
- 闲鱼等「吸顶搜索栏 + 多层嵌套滚动」页面滑动失效：四方向改为优先无障碍标准滚动动作（ACTION_SCROLL）直接驱动可滚动节点，失败回退手势滑动
- 数字同音字纠错：数字命令里「吧→八、衣→一、似→四、久→九」等，修「点击8被听成吧」类误识别
- 热词误纠：单字不再纠成 App 名（「吧」不再被误纠成「贴吧」，也杜绝「音→抖音」隐患）

### perf 优化
- 静音自动释放 20 秒 → 60 秒：刷信息流看内容时不至于频繁还麦

## v0.6

### feat 新功能
- 文字点击：说「打开抖音」/「抖音」等屏幕文字或 App 名，当前页面有就点、没有不响应（零 AI、纯确定性）
- App 名热词：32 个常见 App 名加入热词，ASR 优先识别成正确文字（「抖音」不再被听成「面嗯」）
- 分层匹配：精确命令 → 屏幕文字点击 → 拼音兜底纠错，避免「打开抖音」被误纠成「打开声音」、「动画」被误纠成「上滑」
- 「轻点」改名：点击屏幕命令与「点击抖音」彻底区分，消除冲突

### fix 修复
- 词表超前：移除 15 个声明但未实现的命令（双击/长按/滚动/缩放/音量/静音/截屏/帮助），保证「能说的 = 能做的」
- 熔断误判：被冷却跳过的动作不再计入熔断，避免正常快速操作被误判成循环而释放麦克风
- 退出后残留执行：会话结束（看门狗/静音/退出）后忽略识别线程队列里的残留结果
- 网格返回命令词：主命令定为「退回」（不含数字，从根源避免被数字匹配误吞）

### perf 优化
- 热词权重 4.5 → 8.0：命令词 + App 名识别更准

### test
- 匹配层单元测试适配 matchStrict / matchFuzzy 分层接口

## v0.5（当前）

### feat 新功能
- 「开始会话」按钮：静音释放后无需退出重进，一键重新开始识别
- 「指令说明」按钮：点开查看所有可用指令
- 高通滤波降噪：滤掉 100Hz 以下低频鼓点/轰鸣，提升识别率
- 热词权重 3.0 → 4.5：识别更优先靠向命令词

### perf 优化
- VAD 一句话最长 15s → 3s：避免外放声把你的声音淹没在超长片段里
- VAD 阈值 0.5 → 0.6：少把背景声误当成人声
- 拼音模糊匹配放宽：容错 2 → 3 音节、阈值 0.4 → 0.5

### fix 修复
- 蓝球动画卡住：动画在后台线程触发未切主线程，改为切主线程 + 兜底强制移除
- 横条被状态栏遮挡：下移到状态栏下方
- 重复启动导致重复占麦：加防重入保护

## v0.4（听懂 + 动手 + 反馈）

### feat 新功能
- SenseVoice 离线语音识别 + Silero VAD
- 命令匹配层：exact / contains / pinyin_exact / pinyin_fuzzy 四级纠错（同音字、近音字、多字漏字）
- 执行层：四方向滑动 + 返回 / 主页 / 最近任务
- 顶部状态横条（跨应用悬浮反馈）
- 蓝球滑动动画（实时看到手势正在发生）
- 三层安全熔断：命令冷却 + 误触发熔断 + 看门狗
- 麦克风音源切换为 VOICE_RECOGNITION（自带降噪 / 回声抑制）

## v0.3（可靠释放框架）

### feat 新功能
- 会话制架构：「退出」命令释放麦克风
- 静音自动释放：用 VAD/ASR 判断是否真有人说话（不用音量阈值，避免背景噪音误判）
- 60 秒看门狗：卡死 / 异常时强制释放麦克风

## v0.2（占麦测试）

### feat 新功能
- AudioRecord 占麦测试版

### 实测结论
- 占麦时小爱同学无法唤醒、释放后恢复 → 麦克风独占，证实「会话制 + 严格释放」是唯一正确架构

## v0.1（空壳）

### feat 新功能
- App 骨架 + 前台服务框架
