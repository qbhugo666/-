# 品牌图形标准（LOGO_STANDARD）

> **这是言出法随 logo 的唯一标准**。2026-09-11 二次修订（用户两次点名批评后立铁规矩）。
>
> **核心铁律：logo 图形不手绘、不翻译成几何公式——直接使用从设计规范图抠取的原始像素资产。
> 此前两版手绘（细线 chevron、比例拍脑袋）都不像，用户原话：「你不是多模态模型吗」「把我们真正的 logo 改成两个球了」。**

## 一、资产的唯一来源

| 资产文件 | 内容 | 来源 |
|---|---|---|
| `res/drawable-nodpi/ic_logo_glyph_white.png` | 白色图形（圆点+实心箭头），带抗锯齿 alpha | 从 `design/logo-spec.png` 反白版本面板连通域抠取（133×112 原始像素） |
| `res/drawable-nodpi/tile_logo.png` | 深色圆角方块 App 图标（黑底+白图形，含圆角 alpha） | 规范图「深色模式（主）」方块原样裁切（256×256） |
| `res/mipmap-*/ic_launcher.png` | 桌面启动图标（48/72/96/144/192） | 同上方块裁切，缩放生成 |
| `res/mipmap-anydpi-v26/ic_launcher.xml` | 自适应图标：#0B0B0B 底 + 位图前景 + monochrome | 前景=图形位图 layer-list，40dp/108dp 画布 |

**重新生成资产的脚本**：`_test/make_assets.py` / `make_assets2.py` / `tile_alpha.py`（PIL，按连通域从规范图抠取）。
改 logo = 用户给新设计图 → 跑脚本重新抠 → 替换资产 → 装机对比。**永远不在代码里画 logo 形状。**

## 二、使用规则（代码里的唯一自由度=尺寸和底色）

| 位置 | 实现 | 规则 |
|---|---|---|
| 主页英雄卡 / 关于页 / 聆听状态卡 | `LogoCircleView`（自绘 View） | 品牌蓝正圆 #246BFE + 图形位图，图形宽 = **56.7% 圆直径**（规范深色方块实测 93/164），居中 |
| 主页顶栏 32dp 小方块 | `tile_logo.png` 直贴 | 原样缩放，不重构 |
| 桌面图标 | mipmap PNG + v26 自适应 | 见上表 |

## 三、品牌色彩（规范图印制值）

| 名称 | 色值 | 用途 |
|---|---|---|
| Ink | #111111 | 主要文字/图形 |
| Paper | #F7F7F5 | 背景色/深底上的图形 |
| Deep | #0B0B0B | 深色背景（方块底） |
| Slate | #6F6F68 | 辅助文字 |
| **Accent Blue** | **#246BFE** | 品牌蓝，状态/交互（logo 大圆底色；实测界面图 #1E6CFB 同族，以印制值 #246BFE 为准） |

> App 全局强调色（按钮/胶囊等）目前是 iOS 蓝 #0A84FF；若要统一到品牌蓝 #246BFE 是全局决策，须用户拍板另做。

## 四、图形构成参考（规范图印刷值，仅供文档对照，不再用于代码画形）

圆点直径 32% + 间距 12% + 箭头宽 56% = 100%；箭头为**实心粗壮箭头**（左侧深凹口），不是细线 chevron。

## 五、改动纪律

1. logo 一切改动从设计图资产出发；**禁止**在代码/drawable 里手绘图形（两次翻车记录在案）。
2. 改完必须真机截图与 `design/logo-spec.png` / 界面设计图并排对比验收。
3. 本文件改动属品牌决策，**须用户同意**。
